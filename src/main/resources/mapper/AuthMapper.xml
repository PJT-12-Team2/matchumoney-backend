<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team2.pjt12.matchumoney.domain.auth.mapper.AuthMapper">
    <insert id="save" parameterType="team2.pjt12.matchumoney.domain.user.domain.UserVO" useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO users (
            social_provider, social_id, email, password, nickname, profile_image_url, is_social_login
        ) VALUES (
                     #{socialProvider}, #{socialId}, #{email}, #{password}, #{nickname}, #{profileImageUrl}, #{isSocialLogin}
                 )
    </insert>

    <update id="updatePassword">
        UPDATE users
        SET password = #{password},
            last_modified_time = NOW()
        WHERE email = #{email}
    </update>

    <insert id="insertWithdrawLog" parameterType="team2.pjt12.matchumoney.domain.user.domain.WithdrawLog"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO withdrawal_log (reason, detail, created_time, last_modified_time)
        VALUES (#{reason}, #{detail}, NOW(), NOW())
    </insert>

    <delete id="deleteById" parameterType="long">
        DELETE FROM users
        WHERE user_id = #{userId}
    </delete>

</mapper>