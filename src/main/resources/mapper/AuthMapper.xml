<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team2.pjt12.matchumoney.domain.auth.mapper.AuthMapper">
    <insert id="save" parameterType="team2.pjt12.matchumoney.domain.user.domain.UserVO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            social_provider, social_id, email, password, nickname, profile_image_url,
            created_time, last_modified_time, is_social_login
        ) VALUES (
                     #{socialProvider}, #{socialId}, #{email}, #{password}, #{nickname}, #{profileImageUrl},
                     #{createdTime}, #{lastModifiedTime}, #{isSocialLogin}
                 )
    </insert>

</mapper>